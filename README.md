# Next.js conversion

This folder was converted from a single React file (incorrectly named `index.html`) into a **Next.js + Tailwind** app.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

